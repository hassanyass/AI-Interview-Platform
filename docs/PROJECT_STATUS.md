# Path2Hire Transition — Project Status

Snapshot as of Sub-phase 6C kickoff. Update this after each sub-phase closes.

## Done and verified
| Phase | What it delivered |
|---|---|
| 0 | Module namespace collision fixed, test suite runnable, baseline docs |
| 1 | P0 (candidate identity), P1 (deferred), P2 (no code sandbox) locked |
| 2 | Job/InterviewDefinition/InterviewSection/InterviewQuestion schema, additive |
| 3 | Dual JWT (Supabase + guest), candidate identity resolution & dedup, admin RBAC — most rigorously tested phase so far |
| 4 | Admin CRUD API for jobs/sections/questions, AI question generation (Groq) |
| 5 (5A–5E) | Full HR frontend: role-aware routing, job creation, section config, question editor, publish flow — all verified against the real backend, real browser, no mocking |
| 6 (6A–6D) | Invitation CRUD + shared `get_or_create_job_application`/`get_or_create_candidate_profile` services + console notification stub + `public_access_token` lazy generation (6A); personalized OTP redemption (`/invitations/{token}` + `/redeem`), two-phase commit (VERIFIED durable independent of session/LiveKit success), idempotent re-redeem, hard rejection of guest tokens and email mismatches (6B); public link self-registration, same dedupe guarantee, one new `InterviewSession` per registration (6C); frontend for both flows — `/invite/:token` (OTP code entry + magic-link auto-continue via a `session`-watching effect) and `/apply/:token` (guest registration, scoped `GuestOrAuthRoute`) (6D). **Caveat:** Flow A's magic-link redirect fix (`emailRedirectTo`) is mechanically verified — the outgoing request was intercepted and confirmed to carry the correct `redirect_to` URL — but not yet behaviorally confirmed end-to-end; pending a check of the Supabase project's Redirect URL allowlist and a fresh live OTP test once its email rate limit clears. |
| 7 (7A–7F) | B2B ordered-question runtime: `/load` pipeline sourcing HR-approved Job/InterviewDefinition/InterviewSection/InterviewQuestion content, ordered core-question walk (generalized from the legacy single-question flow), deterministic follow-up cap + time-tier enforcement (no longer advisory-only), legacy-format adapter for pre-Phase-7 sessions, and publish-validation gaps closed (zero-question sections, adversarial first-turn skip) |
| 9 (9A–9F, 9I) | Schema bridge for CODING/MCQ (`InterviewQuestion.config`/`eval_criteria` JSONB + validation at all 4 write paths) (9A); runtime generalization — ordered core-question walk honors admin-configured `InterviewSection.order_index` across all three types, per-type follow-up caps (VERBAL up to 2 time-tier-throttled, CODING/MCQ hard 0) (9B); candidate submission handling — `SUBMIT_CODE`/`SUBMIT_MCQ_ANSWER` data-channel commands, `REQUEST_HINT` ported to the ordered flow (9C); evaluation logic — `eval_criteria` now reaches the agent runtime and the final-evaluation evidence in each type's own native shape (VERBAL bands, CODING complexity/edge-case fields, MCQ explanation-only), not reshaped to match one another (9D); type-specific prompts (`CORE_CODING_QUESTION_PROMPT`/`CORE_MCQ_QUESTION_PROMPT`) plus a deterministic guard preventing CODING/MCQ from completing via a bare LLM `TRANSITION` (9E); backend publish-readiness confirmed live — real Groq generation + real API calls against the running server for both types, `publish_job`'s stopgap intentionally still in place pending 9G/9H (9F); full integration verification mirroring 7F, both types end-to-end including the mid-walk `TRANSITION`-guard (9I). **113/113 passing at last full run.** |
| — | Job-level interview `language` (en/ar) wired end-to-end through to `InterviewSession` |

## Next priority (pivot)
Real-time TTS/STT/LLM interaction hardening is next, ahead of the remaining Phase 9 frontend work and Phases 8/10 — all of which are deliberately queued behind it, not abandoned or blocked on anything.

## In progress
- **Real-time voice pipeline hardening (RT-A / RT-B0 / RT-B1 / RT-B2)** — see `docs/realtime-voice-hardening.md`. RT-A: diagnosis-first exploration, confirmed real code-level root causes for all three reported symptoms (clashing, premature cutoff, intermittent delay), not just hypotheses. RT-B0 (instrumentation, additive, no behavior change): fixed `logging.basicConfig`'s missing timestamp format; added duration/token logging around the Groq LLM call (no SDK metrics event exists for a raw client call, so this is hand-added); wired up the LiveKit SDK's existing `metrics_collected` event for STT/TTS (previously zero listeners anywhere in the codebase). RT-B1 (clashing fix): `_handle_interruption()` now calls `AudioSource.clear_queue()` — RT-A found this was never called, so audio frames already buffered by LiveKit kept playing out past an interruption regardless of the existing generation-invalidation logic. RT-B2 (premature-cutoff fix, one specific contributor): a response that finished generating while the agent was NOT yet speaking is no longer silently discarded outright — it's provisionally preserved and re-validated immediately before it would actually be spoken, discarded only if something genuinely changed again in that window. A response invalidated while the agent WAS already speaking is still discarded outright, unchanged. (The other known premature-cutoff contributor — the VAD/endpoint-delay coalescing race — is a separate, deliberately untouched fixed-delay-floor question, not part of this fix.)
  **Automated tests pass (120/120) — live spoken verification is explicitly DEFERRED, not done.** Per the hardening doc's own verification standard, none of RT-B0/B1/B2 counts as fully closed until a real, live, spoken interview segment confirms the symptoms are actually resolved to the ear — that has not happened yet. Do not read this entry as "hardening complete."

- **Section pacing & waiting room (WR-A through WR-D)** — see `docs/section-pacing-architecture.md`. This was previously missing from this status doc entirely despite being real, substantially complete work already in the codebase — corrected here, not newly built in this pass (except where noted). WR-A: `InterviewSection.config.time_budget_minutes` per-section timer, `_reseed_section_timer()`-style rework of the duration/time-tier logic in `controller.py` to be per-section-relative. WR-B: new `WAITING_ROOM` phase in `state_machine.py` — `VALID_TRANSITIONS`, a fully silent `VALID_ACTIONS_PER_PHASE` entry (no LLM generation while waiting, same shape as `CREATED`/`COMPLETED`), and `VALID_CANDIDATE_CONTROLS_PER_PHASE` gating `PROCEED_TO_NEXT_SECTION` + the universal `END_INTERVIEW` escape hatch. WR-C: controller entry/exit logic (`_transition_out_of_waiting_room`), breaking Phase 9B's invisible `BACKGROUND -> BACKGROUND` section transition exactly at this boundary; auto-timeout handling with both `WAITING_ROOM_CANDIDATE_PROCEED` and `WAITING_ROOM_AUTO_PROCEED` event logging; resume/reconnect back into a persisted `WAITING_ROOM` state. `END_SECTION_EARLY` (the partial reversal of the original "no section skipping" rule) is also real and wired: `_handle_end_section_early()` marks remaining questions `NOT_ATTEMPTED` and routes to `WAITING_ROOM` or `CLOSING`. WR-D frontend: `WaitingRoomScreen.tsx` and `EndSectionEarlyDialog.tsx` — both Himma-styled, both actually mounted in `InterviewWorkspace.tsx`/`InterviewController.tsx` and reading real `sections_progress`/`allowed_controls` state, not stubs.
  **Real backend tests already exist** (`agent/test_skip_regressions.py` — waiting-room entry/exit, auto-proceed timeout, resume-into-waiting-room, both proceed-event types).
  **Live browser + live agent-worker verification: DONE (2026-08-26).** Driven end-to-end against a real published multi-section job (MCQ then VERBAL) through the actual `/apply` flow: registered → intro screen → Start Session → agent joined and greeted → `END_SECTION_EARLY` on the MCQ section → landed on `WAITING_ROOM` showing "Section 1 of 2 complete... completed the Multiple Choice section. Your next section is Verbal" (both labels correctly populated, not blank) → "Continue to next section" → agent correctly resumed into the VERBAL section and asked its question → `END_INTERVIEW` → `CLOSING` → `COMPLETED` → report-loading screen. This closes out WR-A through WR-D as genuinely done, not just code-complete.

- **Candidate intro screen + waiting-room "next section" info (new, 2026-08-26, live-verified)** — the post-registration intro/greeting screen (`IntroScreen.tsx`, shown once for a fresh `CREATED` session, gates the actual LiveKit connect behind a "Start Session" click — zero changes to `voice_adapter.py`/`controller.py`/agent dispatch timing, purely a frontend delay of the existing connect call) and the waiting room's previously-unpopulated "what's next" label (WR-D shipped `WaitingRoomScreen.tsx` with a `nextSectionType` prop but `InterviewWorkspace.tsx` hardcoded it `null` — the realtime state only reports the *currently active* section, which is `None` throughout `WAITING_ROOM` by construction). Resolved via an additive-only backend change: `GET /interviews/{id}` (`InterviewSessionResponse`) now also returns `candidate_instructions` and the definition's ordered `sections` type list, letting the frontend compute both the just-completed and next section labels from `sections_progress.completed`'s index into that static list — no `controller.py` change needed. The intro screen's "End before starting" path uses `POST /interviews/{id}/terminate` (REST), not the data-channel `END_INTERVIEW` control, since no LiveKit session exists yet at that point — also live-verified (shows a dedicated "Interview ended" screen, confirmed the session is marked `TERMINATED` server-side).

## In progress (continued)
- **Phase 9G (HR authoring UI for CODING/MCQ) — built and live-verified (2026-08-26).** `SectionsEditor.tsx` now allows creating CODING/MCQ sections (the `comingSoon` UI gate is lifted for authoring only). `QuestionEditor.tsx` has real type-aware forms reusing the existing `CodingConfig`/`MCQConfig` shapes from `schemas/admin.py` — CODING gets starter_code/supported_languages/constraints/hints inputs, MCQ gets options-with-stable-IDs/correct_answers/is_multi_select. Live-verified in the browser: created a real CODING question ("Two Sum", with starter code and language badges) and a real MCQ question (3 options, correct answer flagged) through the real UI, round-tripped through the real backend, both rendering correctly with their config data on reload. **`publish_job`'s 409 stopgap is deliberately untouched** — confirmed still blocking this exact job (409 Conflict on publish attempt). CODING and MCQ are tracked as separate tracks per `phase9-architecture.md`'s standing rule; do not report them as one "9G done" milestone once one track's candidate-side (9H) verification lags the other's.
- **Phase 9H (candidate submission UI) — built and live-verified (2026-08-26).** The `generate_ui_state()`/`build_core_sections()` bridging fix (Part 1) is done, approved, and live-verified with real captured payloads for both CODING and MCQ. `InterviewWorkspace.tsx`'s candidate UI: MCQ built from scratch (radio/checkbox options, `SUBMIT_MCQ_ANSWER`), CODING repointed to the real `CodingConfig` string shape via `question.config`, plus a real `isTechnical` bug fixed (only checked legacy phase values, missed the ordered flow's `BACKGROUND` phase entirely). Both live-verified end-to-end: real candidate sessions, real submissions, real agent acknowledgment, real advancement to `COMPLETED`. See `docs/phase9-architecture.md`'s 9H section for full evidence.
- **`publish_job`'s per-type 409 stopgap — REMOVED (2026-08-26), both types, per explicit user go-ahead.** Not a temporary bypass this time: the block itself is deleted from `admin.py` (comment left explaining why, and that an equivalent check should be restored if a genuine regression is ever found). `SectionsEditor.tsx`'s `comingSoon` flag was already `false` for both types from 9G. Live-verified for real, no code edit and no bypass: a fresh job with a CODING section and a VERBAL section published successfully through the actual admin UI (`POST .../publish` → `200 OK`, `status: "PUBLISHED"`). **Phase 9 (CODING/MCQ section types) is now genuinely shippable** — 9A through 9I, the Part 1 bridging fix, 9G, 9H, the dedicated visual-mode redesign, and Part 4's stopgap removal are all done and live-verified, not just internally proven under test conditions.
- **CODING/MCQ dedicated visual modes — built and live-verified (2026-08-26).** Previously CODING/MCQ reused `InterviewWorkspace`'s single VERBAL-shaped layout (avatar + shared 340px sidebar + wide bottom control bar). Replaced with three real layout components, chosen by `sections_progress.current_section_type`: `VerbalSectionView.tsx` (the pre-existing shared layout, extracted verbatim — VERBAL and legacy pre-Phase-9 sessions unchanged), `CodingSectionView.tsx` (LeetCode/CoderPad-style split pane — problem left, real editor right, no avatar, collapsible transcript, slim icon-only toolbar), `McqSectionView.tsx` (centered quiz card, no avatar, compact-but-visible transcript strip). `InterviewController.tsx` gained a `variant="compact"` icon-only toolbar mode reusing its existing button/dialog logic rather than a rebuild. A real bug fixed alongside: `isTechnical` (which hides the avatar) never accounted for MCQ, so MCQ sections still rendered avatar-adjacent chrome — now `isOrderedMcq` is checked explicitly. Live-verified end-to-end for both types: real split-pane editor with real starter code, real quiz card with real options, no avatar element present in either (`document.querySelector('[data-character-state]')` confirmed `null`), transcript toggle/compact-strip both working, and — most importantly — the Hint button in both new modes triggers a real spoken (TTS) agent response recorded in the transcript (CODING: a real graduated hint; MCQ: the correct "no hints available for MCQ" response per its own by-design 0-follow-up rule), not just a silently-displayed text. Mode-switching back to `VerbalSectionView` for `CLOSING` after the last section also confirmed working correctly.
  **Environment note:** this session's admin `SectionsEditor`/`JobDetailPage` interactions were intermittently unstable in the browser pane (a form's local expand/open state repeatedly reset mid-interaction). Traced enough to conclude it wasn't caused by this pass's code changes (all confined to `frontend/src/features/interview-session/*`, unrelated to the admin route tree) before working around it by reusing two already-published, already-authored test jobs from the prior 9G/9H session instead of finishing a third fresh one — not fully root-caused, flagged here rather than left silent.

## Not started
- **Phase 8** — results normalization (replace `final_result` JSONB with real `Evaluation`/`Score` tables, HR comparison/ranking views)
- **Phase 10** — cutover (mandatory `definition_id`, drop `InterviewConfiguration` and candidate self-serve, retire legacy adapter)

## Pre-Phase-7 test debt — resolved
The three tests guarding `agent/agent/interview/controller.py` (`test_phase3b.py::test_phase3b`, `test_phase3c.py::test_phase3c_llm_boundary`, `test_phase3e.py::test_phase3e_persistence_roundtrip`) that had been broken since the Phase 0 baseline are fixed. `agent/test_skip_scratch.py::test_skip`'s `GROQ_API_KEY` gap is also resolved. Full suite (as of the last run, post-9B): **95 passed, 0 failed**. No known or accepted failures remain.

## Open unresolved items (tracked in `CURRENT_DECISIONS.md`)
- P1: email provider (console stub in place, functional but not production-ready)
- Invitation expiration policy (currently unenforced, `expires_at=NULL`)
- Whether candidates can retake an interview
- Editing an already-published interview
- Multiple admin users / permissions
- Email templates (blocked on P1)
- Legacy `create_interview` endpoint creates sessions with no `job_id`/`definition_id`/`application_id` — flagged for Phase 10 cutover
- `level` silently defaults to `"mid"` when `Job.seniority` is unset
- Admin login always lands on candidate `/dashboard` first, requires manual navigation to `/admin`

## Known non-blocking debt
- `resume_service.py`: stale/invalid Supabase key warning on startup (doesn't affect RBAC/admin endpoints)
- LiveKit token signing: dev HMAC secret shorter than RFC 7518-recommended 32 bytes (`InsecureKeyLengthWarning`) — fine for dev, needs a real secret before any real deployment
- Leftover test-data rows (jobs, invitations, candidate profiles) accumulated in the dev DB across sessions — harmless, cleanup deferred
- Visual/UX design deliberately deferred (per decision) across all of Phase 5 and will be through Phase 6 — a dedicated design pass is still an unscheduled future phase
- **Recurring dev-environment friction (flagged during Issue 3c's diagnosis, 2026-08-25):** local dev servers have twice muddied a diagnosis in one session — two stale `uvicorn` processes were found both bound to port 8000, hours apart, neither running the current on-disk code (no `--reload`, never restarted after later edits); separately, the frontend Vite dev server had silently died between two verification steps with nothing surfaced about it. Neither is fixed here — not urgent, but worth not losing as a pattern: a documented "how to fully restart both dev servers cleanly" step (or `--reload` for uvicorn / a process-liveness check before trusting a repro) would have saved real diagnosis time and is worth a small future pass.

## Security note
A database password and a Supabase service-role key were briefly exposed in plaintext during Phase 5C debugging (now rotated). Worth a final confirmation that rotation is fully complete and no other stray scratch files containing credentials remain anywhere in the repo, if that hasn't been double-checked recently.
